# VLM 手写体识别项目规划

> 针对新中国成立后手写体历史资料的结构化识别项目
> 版本：v1.0
> 更新日期：2026-04-06

---

## 一、项目背景与目标

### 1.1 背景

你拥有一批新中国成立后的手写体历史资料（如信用社存款凭证、档案等），需要将其数字化为结构化数据。市面 OCR 服务对这类特殊手写体效果不佳，因此计划自建 VLM（Vision-Language Model）识别系统。

### 1.2 目标

```
输入：历史手写体文档图片（如 1980 年代信用社凭证）
输出：结构化 JSON 数据，包含：
{
  "document_type": "信用社存款凭证",
  "date": "1981-02-01",
  "account_holder": "白诠大已人",
  "amount": 1500.00,
  "notes": "福利收费 5 车..."
}
```

### 1.3 适用场景

- 单张高精度识别（研究级分析）
- 批量文档结构化（百张级别）
- 特殊格式文档（表格 + 手写混合）

---

## 二、你的背景分析与定位

### 2.1 优势

| 领域         | 程度 | 说明             |
| ------------ | ---- | ---------------- |
| 经济学       | 专业 | 理解研究数据需求 |
| Stata/Python | 基础 | 有数据处理经验   |
| 研究问题     | 清晰 | 知道需要提取什么 |

### 2.2 需补充的技能

| 技能        | 优先级 | 学习方式     |
| ----------- | ------ | ------------ |
| Python 基础 | ★★★ | 边做边学     |
| 环境配置    | ★★★ | AI 辅助      |
| API 调用    | ★★☆ | AI 提供代码  |
| 模型原理    | ★☆☆ | 了解概念即可 |

### 2.3 与 AI 的合作模式

```
你负责：
├── 定义数据需求（需要提取哪些字段）
├── 提供样本图片（5-10 张典型文档）
├── 验证识别结果（判断是否正确）
└── 学术判断（数据合理性检查）

AI 负责：
├── 环境配置指导
├── 代码编写与调试
├── 模型选择建议
├── 结果解读与优化
└── 错误诊断
```

---

## 三、硬件与软件准备

### 3.1 Mac 配置自检清单

运行以下命令检查配置：

```bash
# 1. 查看芯片型号
uname -m  # arm64 = M 系列，x86_64 = Intel

# 2. 查看内存
memory_pressure | grep "MemSize"

# 3. 查看可用存储
df -h /
```

#### 配置对照表

| 你的配置     | 推荐方案                | 预期性能 |
| ------------ | ----------------------- | -------- |
| M1/M2, 8GB   | 本地轻量模型 + 云端 API | 中等     |
| M1/M2, 16GB+ | 本地 7B 量化模型        | 良好     |
| M3/M4, 16GB+ | 本地部署最佳体验        | 优秀     |
| Intel Mac    | 建议云端方案            | 较慢     |

### 3.2 软件安装清单

#### 阶段 1：基础环境（1-2 小时）

```bash
# 1. 安装 Homebrew（如果未安装）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. 安装 Python 3.11+
brew install python@3.11

# 3. 安装 Git
brew install git
```

#### 阶段 2：AI 工具链（选其一）

**方案 A：Ollama（最简单，推荐入门）**

```bash
brew install ollama
ollama pull qwen2-vl:2b  # 或 llava-phi3
```

**方案 B：MLX（Apple 芯片优化最好）**

```bash
pip3 install mlx-lm mlx-vlm
```

**方案 C：云端 API（无本地部署压力）**

```bash
# 后续配置 API Key 即可
```

### 3.3 项目目录结构

```
Historical Study of Population Migration Patterns/
├── vlm_ocr/                    # 新建目录
│   ├── configs/                # 配置文件
│   ├── models/                 # 模型文件（或云端）
│   ├── samples/                # 样本图片
│   │   ├── train/              # 训练/微调样本
│   │   └── test/               # 测试样本
│   ├── scripts/                # Python 脚本
│   │   ├── 01_explore.py       # 数据探索
│   │   ├── 02_recognize.py     # 识别主脚本
│   │   └── 03_export.py        # 导出 Stata 格式
│   └── output/                 # 识别结果
│       ├── json/               # 原始 JSON
│       └── stata/              # 可导入 Stata 的格式
── docs/
│   └── VLM 手写体识别项目规划.md  # 本文档
└── ...
```

---

## 四、技术路线选择

### 4.1 三种技术方案对比

| 方案                           | 难度   | 成本     | 效果       | 推荐场景           |
| ------------------------------ | ------ | -------- | ---------- | ------------------ |
| **方案 1：云端 API**     | ★☆☆ | 按量付费 | ★★★★   | 快速验证，<500 张  |
| **方案 2：本地轻量模型** | ★★☆ | 免费     | ★★★☆   | 批量处理，隐私需求 |
| **方案 3：微调专用模型** | ★★★ | 时间成本 | ★★★★★ | 大量同类文档       |

### 4.2 推荐路线图

```
第 1-2 周：云端 API 验证可行性
├── 选择 2-3 个 API 服务测试
├── 用 10 张样本验证效果
└── 确定字段提取模板

第 3-4 周：本地部署探索
├── 安装 Ollama/MLX
├── 测试轻量模型（Qwen2-VL-2B）
└── 对比本地 vs 云端效果

第 5-8 周：批量处理与优化
├── 构建批量处理脚本
├── 设计质量检验流程
└── 导出 Stata 可用格式

第 9 周+：微调优化（如需要）
── 收集 50+ 标注样本
├── 微调专用模型
└── 建立自动化流程
```

---

## 五、详细实施步骤

### 阶段 1：环境准备与验证（Week 1）

#### 任务 1.1：配置检查

```bash
# 运行后截图发给 AI
python3 -c "import platform; print(platform.platform())"
python3 -c "import subprocess; print(subprocess.check_output(['uname', '-m']).decode().strip())"
```

#### 任务 1.2：选择并测试 API 服务

**推荐 API 服务对比**：

| 服务商   | 模型    | 价格       | 手写体效果 |
| -------- | ------- | ---------- | ---------- |
| 智谱 AI  | GLM-4V  | ¥0.005/张 | ★★★★   |
| 月之暗面 | Kimi-VL | ¥0.003/张 | ★★★★   |
| 阿里百炼 | Qwen-VL | ¥0.002/张 | ★★★☆   |
| MiniMax  | abab6.5 | ¥0.001/张 | ★★☆     |

**测试脚本**（AI 会帮你写）：

```python
# scripts/01_api_test.py
# 你只需填入 API Key 和图片路径
```

#### 任务 1.3：定义输出模板

与 AI 讨论确定需要提取的字段：

```json
{
  "document_id": "唯一编号",
  "document_type": "文档类型",
  "date": {
    "year": 1981,
    "month": 2,
    "day": 1
  },
  "parties": {
    "account_holder": "户名",
    "handler": "经办人"
  },
  "amounts": {
    "numeric": 1500.00,
    "written": "伍佰元整"
  },
  "notes": "备注内容",
  "seal_text": "印章文字"
}
```

### 阶段 2：本地部署（Week 2-3）

#### 任务 2.1：安装 Ollama

```bash
brew install ollama
ollama serve  # 启动服务
ollama pull qwen2-vl:2b  # 下载模型
```

#### 任务 2.2：测试本地模型

```python
# scripts/02_local_test.py
import requests

response = requests.post('http://localhost:11434/api/generate', json={
    "model": "qwen2-vl:2b",
    "prompt": "识别这张图片中的所有文字",
    "images": ["base64 编码的图片"]
})
print(response.json())
```

#### 任务 2.3：效果对比

| 测试项   | 云端 API | 本地模型 | 结论 |
| -------- | -------- | -------- | ---- |
| 日期识别 | ?        | ?        |      |
| 金额识别 | ?        | ?        |      |
| 手写人名 | ?        | ?        |      |
| 印章文字 | ?        | ?        |      |
| 处理速度 | ?        | ?        |      |

### 阶段 3：批量处理（Week 4-6）

#### 任务 3.1：构建处理脚本

```python
# scripts/03_batch_process.py
# 输入：图片文件夹
# 输出：JSON + Excel + DTA
```

#### 任务 3.2：质量检验

- 随机抽查 10% 人工核对
- 金额字段逻辑校验（大写=小写）
- 日期范围校验（1949-1990）

#### 任务 3.3：Stata 对接

```stata
* 导出的数据可直接使用
use "./vlm_ocr/output/stata/recognized_data.dta", clear
```

### 阶段 4：微调优化（Week 7+，可选）

如果你的文档非常特殊，考虑微调：

#### 任务 4.1：准备标注数据

- 收集 50-100 张典型样本
- 人工标注正确答案
- 格式化为训练数据

#### 任务 4.2：选择微调方案

| 方案        | 成本 | 效果提升 |
| ----------- | ---- | -------- |
| LoRA 微调   | 低   | +10-20%  |
| 全量微调    | 高   | +20-30%  |
| prompt 工程 | 免费 | +5-10%   |

---

## 六、与 AI 高效合作的协议

### 6.1 提问模板

**环境配置问题**：

```
【环境配置】
- Mac 型号：M2, 16GB
- 当前步骤：安装 Ollama
- 遇到的问题：brew install 报错
- 错误信息：[粘贴完整错误]
```

**代码调试问题**：

```
【代码调试】
- 脚本：scripts/02_local_test.py
- 预期：识别图片文字
- 实际：输出为空
- 已尝试：重启 Ollama
```

**结果解读问题**：

```
【结果解读】
- 模型输出：[粘贴 JSON]
- 我的疑问：金额字段是否正确？
- 上下文：这是 1981 年信用社凭证
```

### 6.2 AI 应交付的内容

每次请求后，AI 应提供：

- [ ] 可直接运行的代码
- [ ] 预期输出示例
- [ ] 常见错误及解决方法
- [ ] 下一步建议

### 6.3 检查点

每个阶段完成后，与 AI 确认：

1. 当前阶段目标是否达成
2. 遇到的阻碍是否需要调整方案
3. 下一阶段计划是否需要修改

---

## 七、预算与时间估算

### 7.1 时间投入

| 阶段     | 你的时间   | AI 协助时间 |
| -------- | ---------- | ----------- |
| 环境准备 | 4-6 小时   | 即时        |
| API 测试 | 2-3 小时   | 即时        |
| 本地部署 | 3-5 小时   | 即时        |
| 批量处理 | 8-12 小时  | 即时        |
| 微调优化 | 20-40 小时 | 即时        |

### 7.2 金钱成本

| 项目          | 费用                 |
| ------------- | -------------------- |
| 云端 API 测试 | ¥50-100（初期）     |
| 云端 API 批量 | ¥0.002-0.005/张     |
| 本地部署      | 免费（电费）         |
| 微调训练      | 免费-¥500（云服务） |

---

## 八、风险与应对

| 风险           | 可能性 | 影响 | 应对方案         |
| -------------- | ------ | ---- | ---------------- |
| 本地模型效果差 | 中     | 高   | 回退到云端 API   |
| 处理速度太慢   | 高     | 中   | 夜间批量处理     |
| 特殊字无法识别 | 高     | 中   | 人工标注 + 微调  |
| 环境配置失败   | 中     | 高   | 使用 Docker/云端 |
| 数据隐私问题   | 低     | 高   | 本地部署优先     |

---

## 九、快速启动清单

### 今天可以做的事（2 小时内完成）

```bash
# 1. 检查配置
python3 -c "import platform; print(platform.platform())"

# 2. 创建项目目录
mkdir -p vlm_ocr/{configs,models,samples,scripts,output}

# 3. 准备 5 张样本图片
# 放到 vlm_ocr/samples/test/ 目录

# 4. 注册一个 API 服务（推荐智谱 AI 或 Kimi）
# 获取 API Key

# 5. 让 AI 帮你写第一个测试脚本
```

### 第一周目标

- [ ] 完成环境配置
- [ ] 测试至少 2 个 API 服务
- [ ] 确定字段提取模板
- [ ] 用 10 张样本验证可行性

---

## 十、附录

### A. 常用命令速查

```bash
# 检查 Python 版本
python3 --version

# 安装包
pip3 install xxx

# 运行脚本
python3 scripts/01_xxx.py

# 查看 Ollama 状态
ollama list
```

### B. 推荐学习资源

- [Ollama 文档](https://ollama.ai/docs)
- [Hugging Face 课程](https://huggingface.co/learn)
- [智谱 AI 开发文档](https://open.bigmodel.cn/dev)

### C. 项目日志模板

```markdown
## 2026-04-06
- 完成：环境配置
- 问题：brew 安装慢
- 解决：换了清华源
- 下一步：测试 API
```

---

## 下一步行动

**立即开始**：

1. 回复你的 Mac 配置（芯片型号、内存）
2. 提供 3-5 张样本图片
3. 告诉我你最想优先识别的字段

然后我会帮你写出第一个可运行的测试脚本。
